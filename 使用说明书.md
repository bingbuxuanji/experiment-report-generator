# 实验报告生成器 —— 使用说明书

## 这是什么？

一个能**自动帮你写实验报告**的工具。你只需要：
- 老师给的 Word 模板（.docx 文件）
- 实验相关材料（指导书、笔记、代码等，可选）

工具会自动写出完整报告，并画好流程图。

**不需要会编程，跟着步骤操作即可。**

---

## 第一步：安装（仅首次需要，约5分钟）

### 1.1 安装 Python

1. 打开 [python.org/downloads](https://www.python.org/downloads/)
2. 点击黄色按钮下载
3. 运行下载的文件
4. **重要**：安装界面底部勾选 `Add Python to PATH`
5. 点 Install 等待完成

### 1.2 安装依赖

按 `Win + R`，输入 `cmd` 回车。黑色窗口中输入：

```
pip install python-docx
```

等待出现 `Successfully installed`。

### 1.3 配置 AI（可选但推荐）

双击工具包中的 `setup.bat`，按提示选择：

| 选项 | 说明 | 费用 |
|------|------|------|
| Claude API | 效果最好 | 按量付费 |
| OpenAI API | ChatGPT 同款 | 按量付费 |
| 本地 Ollama | 免费，需要下载模型 | 免费 |
| 跳过 | 离线模式，功能受限 | 免费 |

> 不配置 AI 也能用（离线模式），只是报告部分内容需要手动补充。

---

## 第二步：准备文件

### 必需：Word 模板

老师发的实验报告模板（.docx 格式）。工具会自动识别里面的结构和占位符。

### 可选：实验素材

把所有相关资料放一个文件夹，格式不限：

```
我的实验材料/
├── 实验指导书.pdf
├── 课堂笔记.txt
├── 实验代码/
└── 随便什么文件...
```

素材越详细，生成的报告越准确。

---

## 第三步：生成报告

### 最简单方式：双击运行

双击 `生成报告.bat` → 按提示输入信息 → 等待完成。

### 命令行方式

```bash
# 生成一份报告
python generate_reports_v3.py --name 张三 --id 2024102001 --template 模板.docx --materials 素材文件夹

# 一次生成多份
python generate_reports_v3.py --name 张三 --id 2024102001 --template 模板.docx --exp "实验一:./素材1" --exp "实验二:./素材2" --exp "实验三:./素材3"

# 离线模式（不用 AI）
python generate_reports_v3.py --name 张三 --id 2024102001 --template 模板.docx --materials 素材文件夹 --no-llm
```

---

## 第四步：检查

用 Word/WPS 打开生成的报告，检查：
- 标题是否正确
- 姓名学号是否填入
- 流程图是否正常显示
- 各章节内容是否合理

有问题直接在 Word 里修改即可。流程图大小可以拖拽调整。

---

## 命令速查

| 你想做什么 | 命令 |
|-----------|------|
| 一份报告 | `python generate_reports_v3.py --name 姓名 --id 学号 --template 模板.docx` |
| 有素材 | 加上 `--materials 素材文件夹` |
| 多份报告 | 用 `--exp "标题:路径"` 重复多次 |
| 不用 AI | 加上 `--no-llm` |
| 源码分析 | `--source 代码文件夹` 代替 `--materials` |

---

## 常见问题

**Q: 提示"没有可用的 AI 服务"**
→ 运行 `setup.bat` 配置，或加 `--no-llm` 离线使用

**Q: 流程图不显示**
→ 检查网络；或复制文档中的 Mermaid 代码到 [mermaid.live](https://mermaid.live) 查看

**Q: 内容不准确**
→ 在 Word 中直接改；或修改 `llm_cache_*.txt` 缓存文件后重新生成

**Q: 模板文件路径怎么填**
→ 把文件拖到命令行窗口，路径会自动填入
